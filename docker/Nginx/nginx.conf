events {}

http {
    server {
        listen 80;

        location / {
            proxy_pass http://terrastack-frontend:5000;
        }

        location /api/ {
            rewrite ^/api/(.*) /$1 break;
            proxy_pass http://terrastack-backend:8000/;
        }
    }
}