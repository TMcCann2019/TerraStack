apiVersion: v1
kind: PersistentVolume
metadata:
  name: terrastack-data-pv
  namespace: terrastack
spec:
  capacity:
    storage: {{ .Values.persistence.db.size }}
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /mnt/data/terrastack-db