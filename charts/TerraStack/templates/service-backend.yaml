---
apiVersion: v1
kind: Service
metadata:
  namespace: terrastack
  name: backend-svc
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP