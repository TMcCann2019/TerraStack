apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: terrastack
spec:
  type: ClusterIP
  selector:
    app: db
  ports:
    - port: {{ .Values.service.db.port }}
      targetPort: {{ .Values.service.db.port }}