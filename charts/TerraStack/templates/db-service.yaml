---
apiVersion: v1
kind: Service
metadata:
  name: db-svc
  namespace: terrastack
spec:
  selector:
    app: db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: {{ .Values.service.db.type }}