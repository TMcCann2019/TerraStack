apiVersion: v1
kind: ConfigMap
metadata:
  name: terrastack-config
  namespace: terrastack
data:
  DB_HOST: db
  DB_NAME: terrastack
  DB_PORT: "5432"
  BACKEND_HOST: backend
  BACKEND_PORT: "8000"