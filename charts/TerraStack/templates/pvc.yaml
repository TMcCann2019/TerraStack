apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
  namespace: terrastack
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.db.size }}